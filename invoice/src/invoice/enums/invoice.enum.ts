export enum EInvoiceType {
  additions = 'additions',
  extension = 'extension',
}

export enum EInvoiceStatus {
  created = 'created',
  issued = 'issued',
  pending = 'pending',
  confirmed = 'confirmed',
  canceled = 'cancelled',
}
